import * as Styled from "./styled";

export default function StartChat() {
  return (
    <Styled.Container>
      <Styled.Heading>Start a new chat or select one</Styled.Heading>
    </Styled.Container>
  );
}
